export enum ESearchValue {
	MULTI = 'multi',
	MOVIE = 'movie',
	KEYWORD = 'keyword',
	PERSON = 'person',
}

export enum EResults {
	OK = '0',
	NG = '1',
}

export enum EActionWatchlist {
	ADD = 'add',
	REMOVE = 'remove',
}

export enum EStatusWatchlist {
	ADDED = 1,
	UPDATED = 12,
	REMOVED = 13,
}
